name: Validate Code

on:
  push:
    branches:
      - "tyrone"

jobs:
  run-tests:
    name: Unit tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Test
        run: python manage.py test BrainBusterApp
